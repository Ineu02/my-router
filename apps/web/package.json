{
  "name": "@router/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "3D/WebGL infrastructure control plane for the LLM router",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "preview": "vite preview --port 3000"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/three": "^0.171.0",
    "vite": "^6.0.7"
  }
}
